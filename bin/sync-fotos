#!/bin/sh
rsync -a -v -r \
  --ignore-existing \
  --delete \
  --compress \
  --exclude '.wdmc' \
  /Users/stevepsharpe/Pictures/Photos-Library.photoslibrary/ \
  /Volumes/Public/Shared\ Pictures/Photos-Library.photoslibrary/
